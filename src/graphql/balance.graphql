query QueryBalance($publicKey: PublicKey!, $token: TokenId) {
  account(publicKey: $publicKey, token: $token) {
    balance {
      blockHeight
      stateHash
      liquid
      total
    }
    nonce
    tokenId
  }
}
